<div class="mx-auto ">
  <h1 class="font-bold text-4xl mb-8">New resource</h1>
  <%= render "form", resource: @resource %>
  <hr class="my-8">
  <%= link_to 'Back to resources', resources_path, class: "btn btn-secondary" %>
</div>
