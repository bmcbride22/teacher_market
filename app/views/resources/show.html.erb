<div class="mx-auto md:w-2/3 w-full flex">
  <div class="mx-auto">
    <% if notice.present? %>
      <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
    <% end %>

    <%= render @resource %>
    <hr class="my-4">
    <div class="flex justify-between items-center space-x-4">
      <%= link_to 'Edit this resource', edit_resource_path(@resource), class: "btn btn-primary" %>
      <%= button_to 'Destroy this resource', resource_path(@resource), method: :delete, class: "btn btn-secondary" %>
      <%= link_to 'Back to resources', resources_path, class: "btn btn-danger" %>
    </div>
  </div>
</div>